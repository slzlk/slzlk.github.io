# 丽丽 · 前端开发 × AI 应用实践

个人品牌与作品集主页，基于 Vue 3、Vite 和 TypeScript 构建。

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

构建产物输出到 `dist`，推送到 `master` 分支后由 GitHub Actions 自动部署到 GitHub Pages。
